{
  "hooks": [
    {
      "type": "SessionStart",
      "description": "检测 dtg-pay 项目并自动注入技能上下文",
      "enabled": true,
      "prompt": "## dtg-pay 项目自动检测\n\n请按以下步骤检测当前环境：\n\n### 检测步骤\n\n1. **使用检测脚本验证**：执行以下命令\n   ```bash\n   python3 ${CLAUDE_PLUGIN_ROOT}/scripts/detect-dtg-pay.py\n   ```\n\n2. **如果脚本返回 detected=true**：\n   - 执行 `ls -la` 和 `find . -name 'pom.xml' -type f | head -10` 来了解项目结构\n   - 然后向用户报告：\n     ```\n     🎯 检测到 dtg-pay 项目！\n     ✅ 已自动启用 dtg-java-skill 技能\n     ✅ 已加载 Spring Boot 2.7 + Dubbo 3 微服务开发上下文\n\n     💡 可用命令：\n     - /implement - 智能实现新功能\n     - /review - 代码审查\n     - /code-quality - 质量检查\n     - /project-inject - 重新注入项目上下文\n     ```\n\n3. **如果脚本返回 detected=false**：\n   - 静默完成，不输出任何信息\n\n### 手动检测命令\n\n用户也可以手动运行：\n```bash\npython3 ${CLAUDE_PLUGIN_ROOT}/scripts/detect-dtg-pay.py --help\n```"
    }
  ]
}
