# Technical Terms Reference Guide

## Programming Languages
- Rust → Rust (保持原名)
- Python → Python (保持原名)
- JavaScript → JavaScript (保持原名)
- Go → Go (保持原名)
- Java → Java (保持原名)
- C++ → C++ (保持原名)

## Data Types
- string → 字符串
- integer → 整数
- float → 浮点数
- boolean → 布尔值
- array → 数组
- list → 列表
- dictionary → 字典 (Python) / 映射 (Rust)
- map → 映射
- set → 集合

## Core Concepts
- function → 函数
- method → 方法
- class → 类
- object → 对象
- interface → 接口
- struct → 结构体
- enum → 枚举
- module → 模块
- package → 包
- library → 库
- framework → 框架

## Performance & Complexity
- time complexity → 时间复杂度
- space complexity → 空间复杂度
- optimization → 优化
- performance → 性能
- efficiency → 效率
- scalability → 可扩展性
- concurrency → 并发
- parallelism → 并行
- asynchronous → 异步
- synchronous → 同步

## Development Practices
- debugging → 调试
- testing → 测试
- unit test → 单元测试
- integration test → 集成测试
- documentation → 文档
- refactoring → 重构
- version control → 版本控制
- deployment → 部署
- CI/CD → 持续集成/持续部署

## Error Handling
- error → 错误
- exception → 异常
- panic → 恐慌 (Rust) / 异常终止
- catch → 捕获
- throw → 抛出
- handle → 处理
- recover → 恢复

## Algorithms & Data Structures
- algorithm → 算法
- data structure → 数据结构
- recursion → 递归
- iteration → 迭代
- sorting → 排序
- searching → 搜索
- tree → 树
- graph → 图
- linked list → 链表
- stack → 栈
- queue → 队列
- heap → 堆

## Web Development
- API → API (保持原名)
- HTTP → HTTP (保持原名)
- REST → REST (保持原名)
- JSON → JSON (保持原名)
- XML → XML (保持原名)
- frontend → 前端
- backend → 后端
- database → 数据库
- server → 服务器
- client → 客户端
- request → 请求
- response → 响应

## Development Tools
- IDE → 集成开发环境
- compiler → 编译器
- interpreter → 解释器
- debugger → 调试器
- linter → 代码检查工具
- formatter → 代码格式化工具
- version control system → 版本控制系统

## Security
- security → 安全
- authentication → 认证
- authorization → 授权
- encryption → 加密
- decryption → 解密
- vulnerability → 漏洞
- attack → 攻击
- defense → 防御