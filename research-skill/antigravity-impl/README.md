# Antigravity Implementation Plugin

## 简介

这是一个专门用于执行 Antigravity 计划实作的 Claude Code 插件。该插件会根据存储在 `~/.gemini/antigravity/brain` 目录下的 `implementation_plan.md.resolved` 文件，自动执行代码修改和验证工作。

## 安装

插件已安装在：
```
/Users/tommy/.claude/plugins/antigravity-impl/
```

## 使用方法

### 基本用法

在 Claude Code 对话中，使用以下命令触发代理：

```
antigravity-impl [计划名称]
```

例如：
```
antigravity-impl EZPAY-730-3
```

### 触发方式

代理会在以下情况下自动触发：

1. **直接调用**：
   ```
   antigravity-impl EZPAY-730-3
   ```

2. **中文指令**：
   ```
   执行 EZPAY-730-3 的实作计划
   ```

3. **自然语言**：
   ```
   帮我实作 Antigravity 的 EZPAY-730-3 计划
   ```

## 工作流程

```
用户输入计划名称
       ↓
搜索 ~/.gemini/antigravity/brain/**/implementation_plan.md.resolved
       ↓
匹配包含计划名称的文件
       ↓
解析计划内容（问题分析、修改方案、验证步骤）
       ↓
创建实作任务清单
       ↓
按计划执行代码修改
       ↓
运行测试和验证
       ↓
生成完成报告
```

## 计划文件格式

代理期望找到符合以下格式的 `implementation_plan.md.resolved` 文件：

```markdown
# [计划名称] [标题]

## 问题概要
| 问题 | 风险等级 | 影响 |
|------|---------|------|
| ... | ... | ... |

## 问题分析
[详细的问题分析]

## 修改方案
### [MODIFY] [文件路径]
[diff 格式的修改内容]

## 验证计划
### 自动化测试
```bash
[测试命令]
```

## 修改文件清单
| 文件 | 操作 | 问题 |
|------|------|------|
```

## 代理能力

### 核心功能
- ✅ 自动搜索和匹配 Antigravity 计划文件
- ✅ 解析复杂的 Markdown 格式计划
- ✅ 精确应用代码差异（diff）
- ✅ 执行 Maven/Gradle 测试
- ✅ 验证编译状态
- ✅ 生成详细的实作报告

### 支持的操作
- **MODIFY**：修改现有代码
- **DELETE**：删除代码或方法
- **ADD**：添加新代码

### 可用工具
- Bash：执行命令和测试
- Glob/Grep：搜索文件和内容
- Read/Edit/Write：文件操作
- NotebookEdit：Jupyter notebook 编辑

## 实作示例

假设有以下计划文件内容：

```
# EZPAY-730-3 分润计算问题修复计划

## 问题 1：费率计算舍入模式不匹配

### 修改方案
#### [MODIFY] FeeCalculator.java
```diff
-BigDecimal floatingAmount = XXPayUtil.calOrderMultiplyRate(orderAmount, layer.getFloatingRate());
+BigDecimal floatingAmount = XXPayUtil.calOrderMultiplyRateforFee(orderAmount, layer.getFloatingRate());
```
```

代理会：
1. 读取 `FeeCalculator.java` 文件
2. 找到包含 `calOrderMultiplyRate` 的行
3. 替换为 `calOrderMultiplyRateforFee`
4. 验证修改是否正确
5. 运行相关测试

## 输出格式

代理会提供清晰的进度反馈：

```bash
📋 找到 Antigravity 计划：EZPAY-730-3
📄 计划文件：/Users/tommy/.gemini/antigravity/brain/.../implementation_plan.md.resolved

🎯 实作概要：
- 问题数量：2
- 修改文件：2 个文件
- 风险等级：高

📝 修改文件：FeeCalculator.java
✅ 已完成

🧪 开始验证...
✅ 编译成功
✅ 所有测试通过

✨ 实作完成！
```

## 配置

### Model
默认使用 `inherit`，跟随 Claude Code 的模型选择。

### Color
使用 `green`，代表创建和生成。

### Tools
限制使用必要的工具：
- Bash
- Glob
- Grep
- Read
- Edit
- Write
- NotebookEdit

## 故障排除

### 找不到计划文件
```
❌ 未找到匹配的计划文件

💡 可用的计划：
- EZPAY-730-3
- EZPAY-739

请确认计划名称是否正确。
```

### 修改失败
```
⚠️ 修改文件时遇到问题：FeeCalculator.java

问题：代码已变更，diff 不匹配

建议解决方案：
1. 手动检查文件状态
2. 更新计划文件

等待用户指示...
```

## 扩展

如需添加新功能或修改代理行为，编辑：
```
/Users/tommy/.claude/plugins/antigravity-impl/agents/antigravity-impl.md
```

## 相关文件

- Plugin 配置：`/Users/tommy/.claude/plugins/antigravity-impl/plugin.json`
- Agent 定义：`/Users/tommy/.claude/plugins/antigravity-impl/agents/antigravity-impl.md`
- 计划目录：`~/.gemini/antigravity/brain/`

## 版本

- 版本：1.0.0
- 作者：Claude Code
- 创建日期：2026-01-25
